[project]
name = "server"
version = "0.1.0"
description = "Server For Autotest Agentic System"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.119.1",
    "agent",
]

[tool.uv.sources]
agent = { workspace = true }
